/**
 * File Managed by Puppet
 */

library "db_ido_mysql"

object IdoMysqlConnection "ido-mysql" {
  user = "<%= @user %>",
  password = "<%= @password %>",
  host = "<%= @host %>",
  database = "<%= @database %>"
}

